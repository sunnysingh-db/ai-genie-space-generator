# ============================================================
# AI-Powered Genie Space Generator — Configuration
# ============================================================
# Auto-generated by auto_configure() — review and adjust as needed.
# ============================================================

# ── Data Location ──────────────────────────────────────────────────────
catalog: system
schema: workflow

# ── Genie Space ────────────────────────────────────────────────────────
genie_space_name: "Workflow & Job Run Analytics"

# ── AI Model ───────────────────────────────────────────────────────────
# Options: databricks-claude-opus-4-6, databricks-meta-llama-3-1-405b-instruct
llm_model: databricks-claude-sonnet-4-6

# ── Table Filtering ────────────────────────────────────────────────────
exclude_table_patterns:

# ============================================================
# Business Context (auto-generated from schema analysis)
# ============================================================

# Q1: What does your business do?
business_domain: "Databricks platform operations tracking automated workflow jobs across multiple workspaces, monitoring scheduled and triggered job executions, task performance, and run outcomes."

# Q2: What data is tracked in these tables?
data_description: "Automated job configurations with scheduling details, run history showing success and failure outcomes, individual task-level execution breakdowns with timing phases, and full change history of job and task setups over time."

# Q3: Who uses this data and what decisions do they make?
stakeholders_and_decisions: "Platform engineers monitor job reliability and failure rates; operations teams track execution durations and queue times; workspace admins review job configurations and ownership across teams."

# Q4: Any other important context? (optional)
additional_context: "Data covers late 2025 to early 2026. Job trigger types include scheduled, continuous, file arrival, and table updates. Run outcomes include Succeeded, Failed, Error, Cancelled, and Timed Out. Job runs link to individual tasks, compute resources, and configuration history."

# ============================================================
# Sample Questions (auto-generated)
# ============================================================
sample_questions:
  - "Which jobs have the highest failure rate over the past 30 days?"
  - "How has the average job run duration changed week over week?"
  - "What are the most common reasons jobs are failing or being terminated?"
  - "Which workspaces have the most job runs, and how do their success rates compare?"
  - "How much time are jobs spending waiting in the queue before they start running?"
  - "Which scheduled jobs are taking the longest to complete end to end?"
  - "How many jobs are triggered automatically versus run on demand?"
  - "Which tasks within jobs are failing most frequently and causing overall job failures?"
  - "Are there any jobs that consistently time out, and how long do they typically run before stopping?"
  - "How many jobs were created or modified in the last 90 days, and who made those changes?"
