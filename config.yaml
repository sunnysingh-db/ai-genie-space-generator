# ============================================================
# AI-Powered Genie Space Generator — Configuration
# ============================================================
# Auto-generated by auto_configure() — review and adjust as needed.
# ============================================================

# ── Table List (fully qualified: catalog.schema.table) ──────────────
table_list:
  - "system.billing.list_prices"
  - "system.billing.usage"
  - "system.lakeflow.job_run_timeline"
  - "system.lakeflow.job_task_run_timeline"
  - "system.lakeflow.job_tasks"
  - "system.lakeflow.jobs"
  - "system.lakeflow.pipeline_update_timeline"
  - "system.lakeflow.pipelines"
  - "system.compute.clusters"
  - "system.compute.node_timeline"
  - "system.compute.node_types"
  - "system.compute.warehouse_events"
  - "system.compute.warehouses"

# ── Derived Target Schema (for metric view creation) ──────────────
catalog: system
schema: billing

# ── Genie Space ────────────────────────────────────────────────────────
genie_space_name: "Platform Cost & Operations Analytics"

# ── AI Model ───────────────────────────────────────────────────────────
# Options: databricks-claude-opus-4-6, databricks-meta-llama-3-1-405b-instruct
llm_model: databricks-claude-sonnet-4-6

# ============================================================
# Business Context (auto-generated from table analysis)
# ============================================================

# Q1: What does your business do?
business_domain: "Cloud data platform running jobs, pipelines, SQL warehouses, and compute clusters on Azure, tracking usage costs, resource consumption, and workload performance across multiple workspaces."

# Q2: What data is tracked in these tables?
data_description: "Billing costs with historical pricing by product and unit, detailed usage consumption across all platform services, job and pipeline run histories with success and failure outcomes, compute cluster and warehouse configurations, and minute-level resource utilisation including CPU, memory, and network per machine."

# Q3: Who uses this data and what decisions do they make?
stakeholders_and_decisions: "Finance teams track spending by product and workspace; platform engineers monitor job failures and cluster efficiency; operations managers review pipeline reliability; IT leaders benchmark warehouse usage and right-size compute resources."

# Q4: Any other important context? (optional)
additional_context: "Billing pricing history runs from 2013 to late 2025. Usage and run data covers mid-2025 to early 2026. Products span jobs, pipelines, SQL warehouses, model serving, and AI services. Jobs and pipelines link to compute costs and run outcomes. Warehouses scale between 2X-Small and X-Large."

# ============================================================
# Sample Questions (auto-generated)
# ============================================================
sample_questions:
  - "Which product categories are driving the highest consumption costs this month?"
  - "What is the total spend broken down by workspace over the last 90 days?"
  - "Which jobs are failing most frequently and what are the most common reasons for failure?"
  - "How has our overall platform spending trended week over week over the past quarter?"
  - "Which pipelines take the longest to complete and how often do they fail?"
  - "What percentage of our compute usage is serverless versus traditional clusters?"
  - "Which teams or users are running the most workloads and consuming the most resources?"
  - "How much are we spending on AI and model serving products compared to standard compute?"
  - "Which SQL warehouses are being used most heavily and are any consistently scaling up to their maximum size?"
  - "What are the peak hours for compute resource usage and are there machines with consistently high CPU or memory pressure?"
