# ============================================================
# AI-Powered Genie Space Generator — Configuration
# ============================================================
# Auto-generated by auto_configure() — review and adjust as needed.
# ============================================================

# ── Data Location ──────────────────────────────────────────────────────
catalog: sunny_uc
schema: demo

# ── Genie Space ────────────────────────────────────────────────────────
genie_space_name: "Food Delivery & Aviation Analytics"

# ── AI Model ───────────────────────────────────────────────────────────
llm_model: databricks-claude-sonnet-4-5

# ── Table Filtering ────────────────────────────────────────────────────
exclude_table_patterns:
  - "_quality_monitoring_summary"
  - "abac_data"
  - "agent_%"
  - "fin-agent_%"
  - "hr-agent_%"
  - "legal-agent_%"
  - "food_langraph%"
  - "genie_agent_%"
  - "langchain_agent%"
  - "streaming_agent_%"
  - "test_agent%"
  - "monitor_%"
  - "trace_logs_%"
  - "dashboard_baseline"
  - "dashboard_test%"
  - "df_fs"
  - "dummy_data"
  - "features"
  - "source"
  - "car_features"
  - "vs_%"
  - "contract_metadata"
  - "vs_pdf_split"

# ============================================================
# Business Context (auto-generated from schema analysis)
# ============================================================

# Q1: What does your business do?
business_domain: "Multi-domain platform covering food delivery operations (orders, restaurants, drivers, payments, reviews) and aviation operations (flights, bookings, airlines, airports, passengers) with procurement vendor payment tracking."

# Q2: What data is tracked in these tables?
data_description: "Food orders, restaurants, drivers, payments, reviews, users; flights, bookings, airlines, airports, passengers; and procurement vendor invoices with early payment tracking."

# Q3: Who uses this data and what decisions do they make?
stakeholders_and_decisions: "Ops managers optimize delivery times and driver performance; revenue analysts monitor booking fares, occupancy, and procurement payment compliance."

# Q4: Any other important context? (optional)
additional_context: "Orders span Oct 2025–Feb 2026; loyalty tiers: bronze, silver, gold; flight statuses: Completed, Delayed, Cancelled; procurement BUs: Cement, Ports."

# ============================================================
# Sample Questions (auto-generated)
# ============================================================
sample_questions:
  - "What is average delivery time by cuisine type?"
  - "Which drivers have the highest ratings this month?"
  - "What are total orders by delivery status?"
  - "Which airlines have the highest average occupancy rate?"
  - "What is revenue breakdown by cabin class?"
  - "Which restaurants have the most failed orders?"
  - "What are top payment methods by total amount?"
  - "Which cities generate the most food delivery revenue?"
  - "How many early payment instances per business unit?"
  - "What is average flight delay by airline?"
